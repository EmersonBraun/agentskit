---
'@agentskit/adapters': minor
---

feat(adapters): firm up `groq` adapter — explicit `capabilities: { streaming, tools, usage }`, default model `llama-3.3-70b-versatile`, dedicated test suite, and a `groqAdapter` alias.
