---
'@agentskit/adapters': minor
---

feat(adapters): add `bail` adapter (Alibaba Bailian / Qwen) via the DashScope OpenAI-compatibility endpoint. Default model `qwen-max`. Capabilities: streaming, tools, multiModal (Qwen-VL), usage. Exported as `bail`, `bailAdapter`, and `qwen`.
