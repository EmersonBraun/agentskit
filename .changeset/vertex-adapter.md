---
'@agentskit/adapters': minor
---

feat(adapters): add `vertex` adapter — Gemini on Google Cloud Vertex AI. Project + region routing, OAuth2 access tokens (string or `() => Promise<string>`), partner-publisher support (Anthropic / Mistral on Vertex). Capabilities: streaming, tools, multiModal, usage, reasoning (pro models).
