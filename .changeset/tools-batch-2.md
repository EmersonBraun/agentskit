---
'@agentskit/tools': minor
---

feat(tools): nine new integrations:

- `jira` (#434) — search via JQL + create issues. Basic auth.
- `confluence` (#435) — CQL search + create pages. Basic auth.
- `githubActions` (#436) — list workflow runs + dispatch.
- `sentry` (#439) — org / project issue search + resolve.
- `figma` (#441) — file tree + export images.
- `linearTriage` (#442) — list triage queue + assign / set state.
- `hubspot` (#443) — CRM contact search + create deals.
- `airtable` (#444) — list + create records by base id.
- `shopify` (#445) — Admin REST: product search + order listing.
