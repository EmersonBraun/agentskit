/**
 * @deprecated Import from `@agentskit/cli/commands/index` or the package root.
 * Re-exports the split implementation for back-compat; removed in a future major.
 */
export { createCli } from './commands/index'
