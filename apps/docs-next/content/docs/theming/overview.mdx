---
title: Theming
description: AgentsKit components are headless by default. Import the optional theme for a polished chat UI.
---

AgentsKit components are headless by default. Import the optional theme for a polished chat UI.

## Default Theme

```tsx
import '@agentskit/react/theme'
```

Includes light and dark mode support via `prefers-color-scheme` or `data-theme` attribute.

## CSS Custom Properties

Override any token to customize the theme:

```css
:root {
  --ak-color-bubble-user: #10b981;
  --ak-color-button: #10b981;
  --ak-radius: 16px;
  --ak-font-family: 'Inter', sans-serif;
}
```

### Available Tokens

import { TokenTable } from '@/components/theming/color-swatch'

<TokenTable
  rows={[
    { token: '--ak-color-bg', value: '#ffffff', color: '#ffffff', description: 'Page background' },
    { token: '--ak-color-surface', value: '#f9fafb', color: '#f9fafb', description: 'Surface/card background' },
    { token: '--ak-color-border', value: '#e5e7eb', color: '#e5e7eb', description: 'Border color' },
    { token: '--ak-color-text', value: '#111827', color: '#111827', description: 'Primary text' },
    { token: '--ak-color-text-muted', value: '#6b7280', color: '#6b7280', description: 'Secondary text' },
    { token: '--ak-color-bubble-user', value: '#2563eb', color: '#2563eb', description: 'User message bubble' },
    { token: '--ak-color-bubble-assistant', value: '#f3f4f6', color: '#f3f4f6', description: 'Assistant message bubble' },
    { token: '--ak-color-button', value: '#2563eb', color: '#2563eb', description: 'Button background' },
    { token: '--ak-font-family', value: 'System font stack', description: 'Font family' },
    { token: '--ak-font-size', value: '14px', description: 'Base font size' },
    { token: '--ak-radius', value: '8px', description: 'Border radius' },
    { token: '--ak-spacing-*', value: '4-24px', description: 'Spacing scale (xs, sm, md, lg, xl)' },
  ]}
/>

## Dark Mode

Automatic via `prefers-color-scheme`, or force it:

```html
<div data-theme="dark">
  <ChatContainer>...</ChatContainer>
</div>
```

## Fully Custom Styling

Skip the theme entirely and style using `data-ak-*` attribute selectors:

```css
[data-ak-chat-container] { /* your styles */ }
[data-ak-role="user"] [data-ak-content] { /* user bubble */ }
[data-ak-role="assistant"] [data-ak-content] { /* assistant bubble */ }
```
