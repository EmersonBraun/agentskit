---
title: Evals
description: Measure quality with numbers, not vibes. Suites, replay, snapshots, diff, CI reporters.
---

## Suites

- `runEval({ agent, suite })` — run any `EvalSuite` against any async agent fn. [Recipe](/docs/recipes/eval-suite).

## Deterministic replay

- `createRecordingAdapter` + `createReplayAdapter` — bit-for-bit replay. [Recipe](/docs/recipes/deterministic-replay).
- `createTimeTravelSession` — rewind + override + fork. [Recipe](/docs/recipes/time-travel-debug).
- `replayAgainst` — A/B cassette vs different model. [Recipe](/docs/recipes/replay-different-model).

## Snapshots + diff

- `matchPromptSnapshot` — Jest-style with exact / normalized / similarity. [Recipe](/docs/recipes/prompt-snapshots).
- `promptDiff` + `attributePromptChange` — git-blame for prompts. [Recipe](/docs/recipes/prompt-diff).

## CI reporters

- `reportToCi` + `renderJUnit` + `renderMarkdown` + `renderGitHubAnnotations`. [Recipe](/docs/recipes/evals-ci).

## Open format

- `@agentskit/core/eval-format` — portable eval JSON spec. [Specs](/docs/specs).

*Per-primitive deep dives land in step 6 of the docs IA rollout.*

## Related

- [Package: @agentskit/eval](/docs/packages/eval)
- [For agents: eval](/docs/for-agents/eval)
