---
title: '@agentskit/vue — for agents'
description: Vue 3 composable + ChatContainer component.
---

## Install

```bash
npm install @agentskit/vue
# peer:
npm install vue
```

## Primary exports

- `useChat(config): ChatReturn` — Vue 3 composable, reactive via `reactive()` + auto-cleanup on scope dispose.
- `<ChatContainer :config="...">` — headless container using `data-ak-*` attributes.

## Minimal example

```ts
import { useChat } from '@agentskit/vue'

const chat = useChat({ adapter })
// chat.messages, chat.input are reactive
// chat.send(text), chat.setInput(value), etc.
```

## Related frameworks

- [@agentskit/react](/docs/reference/for-agents/react) — canonical contract.
- [@agentskit/svelte](/docs/reference/for-agents/svelte), [@agentskit/solid](/docs/reference/for-agents/solid), [@agentskit/angular](/docs/reference/for-agents/angular).

## Source

- npm: https://www.npmjs.com/package/@agentskit/vue
- repo: https://github.com/AgentsKit-io/agentskit/tree/main/packages/vue
